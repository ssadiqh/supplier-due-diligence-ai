spring:
  application:
    name: supplier-due-diligence-ai

  datasource:
    url: jdbc:postgresql://localhost:5432/supplier_dd_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baselineOnMigrate: true

server:
  port: 8080
  servlet:
    context-path: /

app:
  upload:
    dir: ./data/case-documents
    max-file-size: 10MB

abn:
  lookup:
    url: https://api.abr.business.gov.au/v1/
    api-key: ${ABN_API_KEY:}
    timeout: 5000
    max-retries: 3

http:
  client:
    connection-timeout: 5000
    socket-timeout: 5000

logging:
  level:
    root: INFO
    com.diligence: DEBUG
